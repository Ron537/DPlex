/** CSS variable for the active session count badge background. */
export const STATUS_ACTIVE_BG = 'var(--dplex-status-active-bg)'
/** CSS variable for the active session color (text/dot). */
export const STATUS_ACTIVE_COLOR = 'var(--dplex-status-active)'
